name: Build Mac App

on:
  push:
    branches: [ main ]

jobs:
  build-mac:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller pywebview requests openpyxl opencv-python pynput uiautomation
      
      - name: Build with PyInstaller
        working-directory: baap-emp-productivity-tracker
        run: |
          pyinstaller --name "ProductivityTracker" --windowed --onefile --clean --noconfirm main.py
      
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Mac-App
          path: baap-emp-productivity-tracker/dist/

